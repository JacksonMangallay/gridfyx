# PHP Extra Security Configuration
expose_php = Off

# APACHE Extra Security Configuration
LoadModule headers_module modules/mod_headers.so (if not yet loaded)
ServerTokens Prod
ServerSignature Off
Header set X-XSS-Protection "1; mode=block"
Header set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
Header always append X-Frame-Options DENY
Header set X-Content-Type-Options nosniff
Header set Content-Security-Policy "default-src 'self';"
Header set X-Permitted-Cross-Domain-Policies "none" (optional)
Header set Referrer-Policy "no-referrer" (optional)